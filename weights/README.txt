Model weights go here.

Run this to download a pretrained fire/smoke model:

    python tools/fetch_model.py --list
    python tools/fetch_model.py --model general

It saves to weights/fire_smoke.pt, which is what config.yaml expects.
